{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Phase 0 demo window: core IPC + events only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
