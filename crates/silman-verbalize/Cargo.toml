[package]
name = "silman-verbalize"
description = "Templated natural-language rendering of silman-core feature records"
license = "BSD-3-Clause"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[features]
# The optional LLM verbalizer (Phase 4) will live behind this flag; the
# default build is fully offline.
llm = []

[dependencies]
silman-core = { path = "../silman-core" }
serde = { workspace = true }
